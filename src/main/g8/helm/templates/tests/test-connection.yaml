apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dory.fullname" . }}-test-connection"
  labels:
    {{- include "dory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
